Interactive && login shell? /etc/profile, ~/.bash_profile, ~/.bash_login, and ~/.profile
Interactive && non-login? ~/.bashrc

Non-interactive && --login? /etc/profile, ~/.bash_profile, ~/.bash_login, and ~/.profile
Non-interactive && !--login (?) BASH_ENV

